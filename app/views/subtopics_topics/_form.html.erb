<%= form_with(model: subtopics_topic) do |form| %>
  <% if subtopics_topic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subtopics_topic.errors.count, "error") %> prohibited this subtopics_topic from being saved:</h2>

      <ul>
        <% subtopics_topic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :subtopic_id, style: "display: block" %>
    <%= form.number_field :subtopic_id %>
  </div>

  <div>
    <%= form.label :topic_id, style: "display: block" %>
    <%= form.number_field :topic_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
