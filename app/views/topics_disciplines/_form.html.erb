<%= form_with(model: topics_discipline) do |form| %>
  <% if topics_discipline.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(topics_discipline.errors.count, "error") %> prohibited this topics_discipline from being saved:</h2>

      <ul>
        <% topics_discipline.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :topic_id, style: "display: block" %>
    <%= form.number_field :topic_id %>
  </div>

  <div>
    <%= form.label :discipline_id, style: "display: block" %>
    <%= form.number_field :discipline_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
