<%= form_with(model: disciplines_author) do |form| %>
  <% if disciplines_author.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(disciplines_author.errors.count, "error") %> prohibited this disciplines_author from being saved:</h2>

      <ul>
        <% disciplines_author.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :discipline_id, style: "display: block" %>
    <%= form.number_field :discipline_id %>
  </div>

  <div>
    <%= form.label :author_id, style: "display: block" %>
    <%= form.number_field :author_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
